name: Package

on:
  push:
    branches:
      - containerise

jobs:
  publish:
    name: Publish docker image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build docker images
        run: docker build --tag activities:latest .
